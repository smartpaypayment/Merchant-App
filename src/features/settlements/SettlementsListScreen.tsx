import { PlaceholderScreen } from '@features/shared/PlaceholderScreen';

/** Section 6.11 — implemented in Section 16 build step 7. */
export function SettlementsListScreen() {
  return <PlaceholderScreen titleKey="settlements.title" icon="wallet-outline" step="Build step 7" />;
}
