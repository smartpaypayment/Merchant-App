import { PlaceholderScreen } from '@features/shared/PlaceholderScreen';

/** Section 6.8 — implemented in Section 16 build step 6. */
export function TransactionsListScreen() {
  return <PlaceholderScreen titleKey="transactions.title" icon="receipt-outline" step="Build step 6" />;
}
