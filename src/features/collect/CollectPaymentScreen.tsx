import { PlaceholderScreen } from '@features/shared/PlaceholderScreen';

/** Section 6.6 — implemented in Section 16 build step 5. */
export function CollectPaymentScreen() {
  return <PlaceholderScreen titleKey="tabs.collect" icon="qr-code-outline" step="Build step 5" />;
}
